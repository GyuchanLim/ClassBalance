<h1>New appointment</h1>

<%# TODO - Cleanup %>
<%# Too dirty %>

<%= form_with model: @appointment do |form| %>
  <div>
    <%= form.label :time %>
    <%= form.datetime_field :time %>
  </div>

  <div>
    <%= form.collection_select :client_1, @active_client_list.order(:name), :id, :name, include_blank: :true %>
    <%= form.collection_select :client_2, @active_client_list.order(:name), :id, :name, include_blank: :true %>
    <%= form.collection_select :client_3, @active_client_list.order(:name), :id, :name, include_blank: :true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= link_to "Back", appointments_path %>
